# This is a template file. Actual secrets should be created using kubectl create secret
# or a secrets management system like Sealed Secrets, External Secrets, or Vault

apiVersion: v1
kind: Secret
metadata:
  name: scrolluniversity-secrets
  namespace: scrolluniversity
type: Opaque
stringData:
  # Database
  DATABASE_URL: "postgresql://user:password@postgres:5432/scrolluniversity"
  
  # Redis
  REDIS_URL: "redis://redis:6379"
  REDIS_PASSWORD: "change-me-in-production"
  
  # JWT
  JWT_SECRET: "change-me-to-a-secure-random-string-at-least-32-characters"
  
  # AI Services
  OPENAI_API_KEY: "sk-..."
  OPENAI_ORGANIZATION: "org-..."
  ANTHROPIC_API_KEY: "sk-ant-..."
  
  # Monitoring
  SENTRY_DSN: "https://...@sentry.io/..."
  SENTRY_AUTH_TOKEN: "..."
  NEW_RELIC_LICENSE_KEY: "..."
  
  # Backup
  BACKUP_S3_BUCKET: "scrolluniversity-backups"
  AWS_ACCESS_KEY_ID: "..."
  AWS_SECRET_ACCESS_KEY: "..."
  
  # CDN
  CDN_URL: "https://cdn.scrolluniversity.com"
  
  # CORS
  CORS_ORIGIN: "https://scrolluniversity.com,https://www.scrolluniversity.com"
  
  # Stripe
  STRIPE_SECRET_KEY: "sk_live_..."
  STRIPE_WEBHOOK_SECRET: "whsec_..."
  
  # Blockchain
  BLOCKCHAIN_RPC_URL: "https://mainnet.infura.io/v3/..."
  BLOCKCHAIN_PRIVATE_KEY: "0x..."
  
  # Email
  SMTP_HOST: "smtp.sendgrid.net"
  SMTP_PORT: "587"
  SMTP_USER: "apikey"
  SMTP_PASSWORD: "SG...."
  
  # Supabase
  SUPABASE_URL: "https://....supabase.co"
  SUPABASE_ANON_KEY: "eyJ..."
  SUPABASE_SERVICE_ROLE_KEY: "eyJ..."
