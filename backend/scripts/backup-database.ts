();

}

main
  }(1);s.exitroceserr);
    ped:', Backup fail('\n❌ rror.e    console
 {err)catch (  
  } on)');
  nformatikup ita.json (bacmetada  - backup-og('console.l  
  able)'); avail CLIabase(if Sup-data.sql -and-schemabasedata'  - onsole.log(   ca)');
 table datll  (aackup.jsontabase-b  - daole.log('cons
    ted:');\nFiles creaog('e.lonsol`);
    c${backupDir}ocation: p Lcku`\nBaog(   console.l);
 ete!'ckup Compl'\n✅ Balog(console.
    r);
ata(backupDiMetadupteBackea  await cr  data
 metaeate
    // Crir);
ackupDpabaseCLI(bupWithSu  await back CLI
  th SupabaseQL backup wi   // Try SpDir);

 SON(backut backupToJ    awais)
always workkup ( bace JSON   // Creatr}`);

 Di ${backup Directory:g(`Backupole.loconsory();
    irectckupDt createBa = awaibackupDironst  cy
   rector diupckreate ba // C{
     try 
`);
String()}\n().toLocale${new Dateackup Time: `Blog(sole.
  con}`);seUrlba URL: ${supapabaseSule.log(`
  conso=\n');ckup ==e BatabasDasity llUniver== Scrosole.log('=con
  e<void> {mis: Pro()on mainctifunnc ;
}

asy, null, 2))tadataringify(meN.st, JSOdataFile(metaFileSync.write  fs
.json');etadata'backup-m, irn(backupD = path.joileataFit metadons };

  cnd data'
 a aemncluding sch backup iaseplete databnote: 'Coml'],
    on', 'sqormat: ['js f,
   pe: 'full'kupTyac
    bbaseUrl,
    supaSOString(), Date().toIstamp: new{
    timemetadata = onst 
  coid> {se<vng): PromiupDir: striata(backtadeBackupMecreatunction 
async f  }
}
ase');
-g supaball pm instinstalled: nse CLI is  Supabaake sure M 💡nsole.log(' 
    coage);rr.mess e',ckup failed: baase CLI'  ⚠ Supabg(e.loconsol    r: any) {
ch (er
  } cat  }
  ); MB)`fileSize} (${qlFile}reated: ${sckup cQL ba.log(`✓ S    console
  2);.toFixed( 1024)e / 1024 /ile).sizsqlF.statSync(Size = (fsnst file      co)) {
sqlFilec(xistsSynif (fs.e  
    ;
    }
  r):', stder⚠ Warninglog('      console.) {
  Dumped')r.includes('stderstderr && ! if (     
 `);
 ${sqlFile}"dump -f "supabase db e && bas`cd supaxecAsync(wait eerr } = aout, stdt { std    conssql');
-and-data.emasch'database-(backupDir,  = path.joinst sqlFile   contry {
 ;
  
   CLI...')h Supabasebackup witing SQL eat\n🔧 Crsole.log('on {
  cse<void>ing): PromitrckupDir: spabaseCLI(baupWithSuacknction b
async fuB)`);
}
e} MSizle} (${fileonFiated: ${js creckup\n✓ JSON basole.log(` coned(2);
 1024).toFix / size / 1024le).jsonFic(tSyne = (fs.stat fileSizons
  c));
  kup, null, 2y(bac.stringifONJSonFile, nc(jseFileSy  fs.writ');
-backup.jsondatabaseupDir, 'backh.join(= patjsonFile 

  const   }
`);
    }edle}: Fail ${tablog(`  ✗    console.
  r) {(ercatch `);
    } 0} rowsgth || ?.lenata ${d ${table}:g(`  ✓onsole.lo;
      c= data || []up[table] 
      back   }
      
   ue;incont   
     essage}`); ${error.m${table}:.log(`  ⚠ nsole co {
        if (error)    
  
     ('*');).select.from(tablebase= await supa, error } ataonst { d
      cy {   trables) {
 able of t t  for (const
;
]> = {}ny[ ag,trinrd<sckup: Reco  const ba

  ];'
gramsee_prodar', 'degrademic_calen', 'acat_messages_posts', 'ch 'community',
   _groupss', 'studyevotionries', 'durnal_ent'prayer_joges', crollbad  'sions',
  n_transact'scrollcoi', ns', 'gradesmissio, 'subignments'
    'assents',lmes', 'enrols', 'lectururse_modules', 'cose, 'courrs'use[
    '= ables onst t
  
  cp...');ON backu Creating JS\n📦sole.log('  con> {
se<voidromig): PstrinckupDir: pToJSON(baackuunction b
async f;
}
backupDir 
  return }
 ue });
  : trive { recurs(backupDir,rSync fs.mkdiDir)) {
   (backupexistsSyncif (!fs.
  
  tamp);eskups', timse-bac), 'databacess.cwd((pro path.joinDir =st backup
  conT')[0];('-').splite(/[:.]/g, 'eplacSOString().rtoIDate().stamp = new time
  const e<string> {): Promisectory(upDirteBackon creafuncti
}

async ;a: booleanincludeDatn;
  : booleahemacludeSc';
  inson' | 'csvsql' | 'jmat: 'or
  fpOptions {ckue Ba

interfacKey);pabaseUrl, subase(supateClientse = creapaba';
const su || 'EYBASE_ANON_Kv.VITE_SUPAen = process.supabaseKeyt ';
consL || 'BASE_URPAITE_SUcess.env.Vrl = prosupabaseU
const exec);
ify( promisecAsync =t exil';

consfrom 'utmisify }  proort {cess';
imp_prochildom 'exec } fr
import { rom 'path';* as path fport om 'fs';
ims fr* as fmport ';
i-jssupabasease/@supab } from 'nt createClieort {
impta
 */
dama and luding schease inctabSupabase dayour backup of tes a full ipt
 * Creaup ScrBackase lete Databmp* Co**
 /